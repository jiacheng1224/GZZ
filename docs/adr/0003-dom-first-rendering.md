# ADR-0003：首版采用 DOM/CSS 渲染

- 状态：Accepted
- 日期：2026-08-01

## 决策

首个可玩版本使用 React 组件和 DOM/CSS，不引入 Phaser 或其他 Canvas 游戏引擎。

## 原因

当前交互以卡牌、槽位、文字与按钮为主。DOM 更适合响应式布局、键盘操作、可访问性和浏览器自动化测试。

## 重新评估条件

当产品需要自由移动单位、复杂镜头、大量粒子或 DOM 性能无法满足目标时，再评估独立渲染层。
