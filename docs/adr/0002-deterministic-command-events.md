# ADR-0002：采用确定性命令／事件模型

- 状态：Accepted
- 日期：2026-08-01

## 决策

领域更新使用命令输入、事件输出和可注入的种子随机数。同一初始状态与命令序列必须产生相同结果。

## 原因

确定性是固定局面测试、行动回放、AI 对局基准、断线恢复与线上争议复现的共同基础。

## 影响

- 禁止在规则函数中直接调用 `Math.random()`。
- 每条命令携带操作者和必要参数。
- 事件日志带规则版本，未来升级时提供迁移策略。
