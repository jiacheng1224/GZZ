# ADR-0001：规则核心使用纯 TypeScript

- 状态：Accepted
- 日期：2026-08-01

## 决策

阵型、回合、战术、占旗与胜负逻辑放入无 React、DOM 和网络依赖的 TypeScript 模块。

## 原因

同一套规则需要被 Web UI、自动测试、AI、回放与未来权威服务器共同复用。纯领域层能避免 UI 与服务器产生两套判定。

## 影响

- UI 不能直接修改领域状态。
- 规则引擎需要显式命令、事件和错误类型。
- 视觉反馈必须从领域事件派生。
